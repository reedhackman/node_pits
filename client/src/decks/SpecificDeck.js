import React from "react";
import { A } from "hookrouter";

const SpecificDeck = ({ faction, agenda }) => {
  console.log(faction, agenda);
  return <div>SPECIFICDECK</div>;
};

export default SpecificDeck;
